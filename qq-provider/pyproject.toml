[project]
name = "qq-provider"
version = "0.1.0"
description = "QQ 音乐 provider:QQMusicApi 作为库,包一层 UDS server"
requires-python = ">=3.11"
dependencies = [
    "curl-cffi>=0.15.0",
    "qqmusic-api-python",
]

[dependency-groups]
dev = ["nuitka"]
